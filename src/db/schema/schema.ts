import { account, session, user } from "./auth";
import { likes } from "./likes";
import { categories, models } from "./models";

export const schema = {
  account,
  categories,
  likes,
  models,
  session,
  user,
};
